const CAMPUSQ_ORIGIN = "https://campusq.retriive.com";

/** Sign in for returning CampusQ users. */
export const CAMPUSQ_LOGIN_URL = `${CAMPUSQ_ORIGIN}/sign-in`;

/** Open the live CampusQ app (matches campusq.retriive.com's primary CTA). */
export const CAMPUSQ_APP_URL = `${CAMPUSQ_ORIGIN}/sign-up`;
